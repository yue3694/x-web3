// Placeholder. Overwritten by:
//   pnpm --filter @x-web3/contracts export:abi
// Run that command after `forge build` to populate this file from the
// latest compile artifact.
export const counterAbi = [] as const;
export type CounterAbi = typeof counterAbi;